node_modules/
dist/
*.tgz
.DS_Store

# Saída de build do MGR
/dist/mgr-runtime/

# Nota: .specs/ é a fonte de verdade do handoff — NÃO ignore em projetos que usam o MGR.
exemplos/

# Cobertura de testes
lcov.info
coverage/

# Cache de análise do spec-init
.spec-init/cache/

# Auto-install do MGR (dogfooding) — conteúdo gerado por `mgr install`
.claude/

# Cache/contexto do spec-create
.specs-cache/

# Artefatos SDD mantidos apenas localmente, por decisao do autor (2026-07-14).
# Ficam no disco (as skills spec-create/spec-execute dependem de docs/sdd/ + CONSTITUTION.md),
# mas fora do versionamento. docs/adr/ SEGUE versionado.
specs/
docs/sdd/
